node_modules/
.env
*.log
.DS_Store
docs/media/*.mp4

# Local CI pass marker (scripts/ci-local-verdict.sh) - per-machine, never committed
.local-ci-pass.json
# Local CI pass cache (scripts/check-ci-local.mjs) - per-machine, never committed
.local-ci-cache.json
